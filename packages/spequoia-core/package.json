{
  "name": "spequoia-core",
  "version": "1.0.0",
  "description": "Core parser, linter and spec model",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "tsc && tsup src/index.ts --format cjs,esm --sourcemap --out-dir dist",
    "lint": "eslint src/**/*.ts",
    "test": "vitest"
  },
  "author": "pckerneis",
  "license": "MIT",
  "dependencies": {
    "ajv": "^8.17.1",
    "yaml": "^2.7.1"
  }
}
