{
  "name": "spequoia-core",
  "version": "1.0.0",
  "description": "Core parser, linter and spec model",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "tsup src/index.ts --format cjs,esm",
    "lint": "eslint src/**/*.ts",
    "test": "vitest"
  },
  "author": "pckerneis",
  "license": "MIT",
  "dependencies": {
    "ajv": "^8.17.1",
    "yaml": "^2.7.1"
  }
}
