{
  "name": "spequoia-core",
  "version": "1.0.0",
  "description": "Core parser, linter and spec model",
  "main": "index.js",
  "scripts": {
    "build": "tsup src/index.ts --dts --format cjs,esm",
    "lint": "eslint src/**/*.ts",
    "test": "vitest"
  },
  "author": "pckerneis",
  "license": "MIT",
  "dependencies": {
    "yaml": "^2.7.1"
  }
}
